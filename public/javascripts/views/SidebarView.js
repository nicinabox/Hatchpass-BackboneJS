// Generated by CoffeeScript 1.4.0
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  App.Views.SidebarView = (function(_super) {

    __extends(SidebarView, _super);

    function SidebarView() {
      return SidebarView.__super__.constructor.apply(this, arguments);
    }

    SidebarView.prototype.el = $('#sidebar');

    SidebarView.prototype.events = {
      "click .panel-nav a": 'setActivePanel'
    };

    SidebarView.prototype.initialize = function() {
      return this.setActivePanel();
    };

    SidebarView.prototype.setActivePanel = function(event) {
      var $nav_item, $target, target;
      if (event && event.type === 'click') {
        target = event.currentTarget.hash;
      } else {
        target = window.location.hash || '#domains';
      }
      $target = $(target);
      $nav_item = this.$(".panel-nav a[href^=" + target + "]");
      this.$('.panel').hide();
      $nav_item.siblings().removeClass('active');
      $nav_item.addClass('active');
      return $target.show();
    };

    return SidebarView;

  })(Backbone.View);

}).call(this);
